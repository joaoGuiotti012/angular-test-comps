/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HelperClienteModalDirective } from './helper-cliente-modal.directive';

describe('Directive: HelperClienteModal', () => {
  it('should create an instance', () => {
    const directive = new HelperClienteModalDirective();
    expect(directive).toBeTruthy();
  });
});
